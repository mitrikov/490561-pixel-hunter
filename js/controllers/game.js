/* import gameControllers from './games/games';
import Screen from '../screen';

const controllers = (template, gametype) => {
  switch (gametype) {
    case `game-1` : template.setControllers(() => controllers());
  }
};

export default controllers; */

